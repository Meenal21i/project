import { Given } from '@cucumber/cucumber';

Given('User is logged into the HRMS application', async function () {
  // Actual login implementation is handled in support/hooks.ts -> Before hook
  // This is used as placeholder so that it can be used in feature files to increase readability.
});
